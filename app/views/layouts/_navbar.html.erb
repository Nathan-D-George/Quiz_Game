<nav class="navbar navbar-expand-lg navbar-dark bg-dark nav">
  <div class="container-fluid col-8">
    <a class="navbar-brand" href="#"> <h3> Quiz </h3> </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/about">About <%= fa_icon "info-circle" %> </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="#">Link</a>
        </li>
      </ul>
      <% if current_user.present? %>
        <%= link_to current_user.email, destroy_user_session_path, class:'nav-link active', style:'color: white' %>
      <% else %>
        <%= link_to "Log In", new_user_session_path, class:'nav-link active', style:'color: white' %>
      <% end %>
    </div>
  </div>
</nav>